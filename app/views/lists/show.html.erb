<h1>CourtList</h1>
<form>
  <fieldset>
    <legend>New Matter</legend>
    <label>
      Docket Number
      <input type="text" name="docket_number" />
    </label>
    <button>Save</button>
  </fieldset>
</form>




<%= @list.room %>
<script>window.list_id = <%= @list.id %></script>
<br>

<section class="matters">
</section>

<script type="template" class="template">
  <div class="row <%%= matter.get('done') ? "done" : "" %>">
    <div class="small-4 large-6 columns">docket_number: <%%= matter.get('docket_number') %></div>
    <div class="small-8 large-6 columns">disposition: <%%= matter.get('disposition') %>
    <input type="checkbox" <%%= matter.get('done') ? "checked" : "" %>/>
    <span>&times;</span></div>
  </div>
</script>